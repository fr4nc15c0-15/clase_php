{# Parcial reutilizable para el formulario de categorías. #}
{{ form_start(form) }}
    {{ form_row(form.nombre) }}
    {{ form_row(form.descripcion) }}
    <button>{{ button_label|default('Guardar') }}</button>
{{ form_end(form) }}
